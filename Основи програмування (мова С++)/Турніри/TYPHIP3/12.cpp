//#include <iostream>
//#include <iomanip>
//#include <cmath>
//
//using namespace std;
//int main() {
//    int n, x, y, z;
//    double s, p, a, b, c;
//    cin >> n >> x >> y >> z;
//    if ((1 <= n && n <= 1000) && (1 <= x && x <= 100) && (1 <= y && y <= 100) && (1 <= z && z <= 100)){
//        a = (double)n / (x + y + z) * x;
//        b = (double)n / (x + y + z) * y;
//        c = (double)n / (x + y + z) * z;
//        p = n / 2.0;
//        s = sqrt(p * (p - a) * (p - b) * (p - c));
//    }
//    cout << fixed << setprecision(4) << s << endl;
//    return 0;
//}